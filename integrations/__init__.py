"""Integration framework — SCAFFOLD ONLY (handoff Phase 5).

Do NOT implement adapters now. The base interface and the `integrations` config
table are in place so turning a project on later is additive: write one adapter,
add one config row.
"""
