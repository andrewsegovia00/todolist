# Core bot / router / scheduler
discord.py>=2.4,<3.0
APScheduler>=3.10,<4.0

# Database
supabase>=2.5,<3.0

# Config / env
python-dotenv>=1.0,<2.0

# Dashboard (functional, neutral — server-rendered)
Flask>=3.0,<4.0

# Optional: Python Claude Agent SDK path (AGENT_MODE=sdk).
# The default AGENT_MODE=cli shells out to `claude -p` and needs nothing here.
# claude-agent-sdk>=0.1.0

# Dev / test
pytest>=8.0,<9.0
